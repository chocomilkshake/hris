# Enable rewrite engine
RewriteEngine On

# Make sure the request is not for a real file or directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Remove .php extension
RewriteRule ^([^/]+)$ $1.php [L]
